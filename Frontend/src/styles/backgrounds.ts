import {COLORS} from './colors';
import {ColorBackground} from '../types/styles/colorBackground';

export const BACKGROUND_COLOR: ColorBackground = {
  primary: COLORS.gray70,
  secondaryLight: COLORS.gray50,
  secondaryDark: COLORS.gray60,
  accent: COLORS.yellow20,
};
