import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  containerDashed: {
    position: 'relative',
    alignItems: 'center',
    justifyContent: 'center',
    borderStyle: 'dashed',
  },
  hand: {
    position: 'absolute',
  },
});
