export const getItemDynamicStyles = (
  itemWidth: number,
  itemHeight: number,
  itemColor: string,
) => {
  return {
    width: itemWidth,
    height: itemHeight,
    backgroundColor: itemColor,
  };
};
