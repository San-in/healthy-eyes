import {StyleSheet} from 'react-native';
import {PADDING_INPUT_BASIC} from '../../../../../styles/vars';

export const styles = StyleSheet.create({
  icon: {
    position: 'absolute',
    zIndex: 5,
    top: '50%',
    left: PADDING_INPUT_BASIC,
  },
});
